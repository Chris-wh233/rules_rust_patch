# rules_rust_patch

Local patch set for adapting `rules_rust` 0.42.1 to LoongArch64.

Current files are under:

- `loongarch64_adaptation/`

This repository is used as a patch handoff/staging area. Changes are committed
locally only when requested, and are not pushed upstream unless explicitly
requested.
